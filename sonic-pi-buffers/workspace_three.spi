require 'treetop'
Treetop.load 'PATH_TO/grammar/forge_grammar.tt'

parser = ForgeGrammarParser.new
result = parser.parse('1soo_*2*4/6(4,5)')

if !result
  puts parser.failure_reason
else
  puts result.elements
  puts result.content
end