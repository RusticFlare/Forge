sounds = {
  :name => :list,
  :content => (ring
               {
                 :name => :sym,
                 :content => :drum_heavy_kick,
                 :mods => []
               },
               {
                 :name => :sym,
                 :content => :drum_cymbal_closed,
                 :mods => []
               }
               )
}

live_loop :demo do
  play_data_structure sounds
end